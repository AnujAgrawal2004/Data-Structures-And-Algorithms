package com.company;

public class Product {
    public int prod(int n)
    {
        if(n%10==n)
            return n;
        return (n%10)*prod(n/10);
    }

    public static void main(String[] args) {
        Product p=new Product();
        System.out.println(p.prod(562));
    }
}
